#define  _CRT_SECURE_NO_WARNINGS

#include <stdlib.h>
#include <conio.h>

int main()
{
	int W = 15, S = W * W, * m, z[2] = { 0 }, l = 3, c = 'D', C, i, f, * p;
	for (srand(m = calloc(S, 4)), C = m[i] = -1; C - 27; _sleep(100))
	{
		if (_kbhit())
		{
			C = _getch() & 95, C - 65 && C - 68 && C - 83 && C - 87
		}
	}

	return 0;
}